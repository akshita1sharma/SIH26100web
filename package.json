{
  "name": "sih26100web-root",
  "version": "1.0.0",
  "description": "Root package to run both frontend and backend",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && python -m uvicorn main:app --reload\"",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
